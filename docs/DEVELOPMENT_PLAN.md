# Development Plan — Hex Colony

Активный план. Здесь только **текущее состояние и следующая стадия**.
История закрытых стадий вынесена в `docs/archive/` — туда идут за ответом «почему сделано так»,
а не за планом. Дизайн-контракт: `MVP_hex_colony.md`.

Работаем по одной стадии. Не забегать вперёд и не реализовывать механики следующих стадий.
Стадия закрывается только после реализации, проверки и обновления этого файла.

---

## Состояние на 05.09.2026

- Игровой цикл замкнут. Стадии **M1–M22 закрыты**, записи о них — в архиве.
- Последнее закрытое: M22 «Река перестаёт быть линейкой» — решение человека 05.09.2026 вне курса
  визуального апгрейда. Осевая линия ленты съехала с решётки (`RiverCourse.Gate`/`Bend`, снос по
  хэшу пары плиток и самой плитки), кэш меша на «маску + ширину» снят — меш строится на плитку,
  ширина растёт по течению (`RiverWidth`, `TileData.RiverFlow`/`RiverDownMask`), у реки своя
  частота шума и штраф третьему шагу подряд в одном направлении. Замер на 200 сидах: пробеги
  длиной 3+ упали с 30.1% до 10.0%, средняя длина русла 10.56 → 12.11 плитки на карту (граница
  8–18 держится).
- Перед ним: M21 «Здание Метрополии на модели» — на гексе Метрополии встал замок
  `building_castle_blue.fbx`, масштаб по следу на земле (`FootprintScale`), не по габариту.
- Правка вне стадии 01.09.2026 по решению человека: контракты приходят с паузой 5–20 секунд,
  сданный крафт летит из клетки склада в карточку контракта, карточка появляется и уходит
  анимацией. Разбор — в `docs/archive/changelog.md`.
- Правка вне стадии 04.09.2026 по решению человека: сообщения стали попапами над объектами
  (`PopupView`), столбец плашек в углу снят, открытие плитки подтверждают попапом с ценой
  и зелёной галочкой. Разбор — в `docs/archive/changelog.md`.
- Правка вне стадии 05.09.2026 по решению человека: заведено главное меню (строка «нет главного
  меню» из 1 MVP-спеки снята) — «Случайная карта», «Ввести сид» (экранная цифровая клавиатура,
  `EventSystem`-а в проекте по-прежнему нет), «Выход» (скрыта в WebGL-сборке). `GameSession`
  выключен в сцене по умолчанию и включается выбором карты; `GameInput` вынесен на свой объект
  `Input`, чтобы меню могло читать клики до старта партии. Круглая кнопка-шестерёнка рядом со
  складом открывает паузу («Продолжить», «Рестарт карты», «Выход в главное меню»); пока она
  открыта, поле и таймеры партии стоят. `SessionSeed.SkipMenu` пропускает меню при перезагрузке
  сцены с уже выбранным сидом (рестарт с финального экрана и из паузы). Новый меш `GearMesh` и
  материал `Gear.mat` — тем же приёмом, что корона финального экрана. Разбор — в
  `docs/archive/changelog.md`.
- Тесты: EditMode **330/330** (было 326: `RiverMeshBuilderTests` переписан под новую геометрию —
  снят кэш, добавлены проверки шва ворот, инварианта сноса и ширины на шве).
- Числа баланса пересняты замером на 200 сидах: потолок очков 4074 против 4739 у прежнего
  генератора на том же поле (−14%), запас на совершенный проход 2115 против 2198. Таблица целиком —
  в архиве. Числа стартовые.
- Ограничения прототипа сняты решением человека 29.08.2026, детали — в `AGENTS.md`.
- Курс на визуальный апгрейд, заданный 01.09.2026, стадиями пройден: незакрытым в нём остался
  только пункт 5 — постобработка «с упором на красоту» после ручных тестов.

## Курс: визуальный апгрейд (решение человека 01.09.2026)

Референс: `docs/refs/ui_and_style.png` — на него ссылается весь курс, смотреть перед стадией.

Игра идёт к картинке референса: яркий читаемый toon-3D в его гамме. Референс — рисованный
2.5D-арт, не кадр сцены, и повторяется не буквально: цель — насыщенность, мягкий объём, толстые
борта плиток, вода под картой и панельный UI. Шесть решений человека, они и разбиты на стадии:

1. ~~Вода — красивая подложка самой карты. Водный биом возвращается **непроходимым**.~~ — M17.
2. ~~На финальном экране заводятся кнопки. Правило «кнопки рестарта нет» переписывается.~~ — M19.
3. ~~Шрифты импортируются и делаются красивыми.~~ — M18: TMP и два SDF-ассета из `Inter`.
4. ~~Спрайты заводятся.~~ — M18, но не файлами и в итоге даже не спрайтами: карточки рисует
   шейдер `Game/UiPanel`. Решение человека.
5. Постобработка делается с упором на красоту; после ручных тестов решаем дальше.
6. Целимся в читаемый яркий toon-3D в гамме референса, а не в painted-look.

Порядок стадий не произвольный: свет и гамма шли первыми, потому что палитру воды, тон панелей
и цвет моделей подбирают против готового тона, а не наоборот. Рельеф шёл раньше воды: уровень
воды имеет смысл только при известных высотах. UI идёт после поля, финальный экран — последним,
он единственный меняет правила.

## Следующая стадия — не назначена

Все стадии, которые были расписаны, закрыты, и новую придумывать за человека не нужно: следующая
назначается его решением. Что лежит нерешённым на сегодня — пункт 5 курса выше (постобработка
после ручных тестов), повозка, вынутая из M20 вместе с пятым шагом, объём русла и вода шейдером
`Game/Water` для реки (следующий разговор про картинку, если после M22 она всё ещё не устроит),
и список открытых хвостов ниже. Хвост — не заявка на стадию: он ждёт решения, а не починки
по своей инициативе.

## Открытые хвосты

Перенесены из M14, M16, M17, M19, M20, M21 и M22, ждут решения или ручной проверки. Это **не** список
дефектов к починке по своей инициативе — трогать только по решению человека.

- **Палитра привязана к постобработке.** Альбедо биомов подобрано решателем против финального
  кадра, и насыщенность профиля (+22) делает часть работы. Меняешь постобработку — палитру
  надо пересчитывать решателем, а не править по одному числу. Цвета, заведённые в M17 —
  дно `0.62/0.60/0.55` и Метрополия `0.70/0.56/0.42`, — подобраны глазом против кадра, а не
  решателем: при следующем пересчёте они идут в общий котёл.
- **Истощённая плитка на новой гамме уходит в хаки.** Читается как выработанная, красивой не
  стала. Рычагов два: `depletedFog` и `depletedFade` на префабе.
- **Отбор карт смещает выборку по воде.** По сырому шуму воды 13.3%, после отбора — 9.1%: порог
  `MinPassableShare` рубит карты с большим заливом. Рычаг один, опускать его дальше 0.65 без
  плейтеста не стал.
- **Дюны читаются бурыми комьями.** Холмик из M16 объёмный, но `sandDecor` тёмный. Правится
  решателем вместе со всей палитрой, а не одним числом.
- **Преломления фона под стеклом нет.** Карта под панелью не смещается и не размывается: для
  этого нужна текстура сцены, то есть перевод канваса в Screen Space — Camera и opaque-текстура
  URP. Отложено решением человека.
- **У панели два прохода отрисовки** — гашение фона отдельным проходом-умножением. Панелей три
  десятка, замера на устройстве не делалось.
- **Лицензия на монету не подтверждена.** `coin.fbx` и `coinC_2.png` лежат в `Models/` без файла
  лицензии рядом. Второй такой хвост после стеков ресурсов, правило то же.
- **Перил на площадке развилки нет.** Они идут вдоль пролётов, а середина моста на речной плитке
  с тремя лучами остаётся открытой. Видно только на развилке прямо над руслом, редкий случай.
- **Мост в игре глазами не смотрен.** Обе формы собраны превью в редакторе на синтетическом поле
  и там сняты; в партии дорога через реку не строилась. `degraded`.
- **На выходе из площадки узла боковина переходит с откоса на стенку уступом** ~0.055: узел
  строит откос, а луч с лестницей — вертикаль. Видно на снимке, мелко, не чинил.
- **Декор стоит поверх дороги.** `TileView` расставляет деревья и камни, зная только о русле,
  и дорога, построенная позже, проходит сквозь них. Было и раньше, но объёмная насыпь это
  подчёркивает. Рычаг — учесть маску маршрута при раскладке декора; не трогал.
- **Числа насыпи подобраны глазом по одному кадру.** `SurfaceLift` 0.05 и ширина 0.22: на 0.035
  откос выходил в два-три пикселя и дорога читалась плоской заливкой. Числа стартовые.
- **Плашка осталась однострочной.** В референсе она в три строки, с множителем и временем.
  Множителя в нашей экономике нет, трёхстрочный вид не делали.
- **Цены плитки в HUD нет** — решение человека. Если по плейтесту окажется, что она нужна,
  договорились вернуть её пилюлей рядом с карточкой очков.
- **У кнопки финального экрана нет состояния «нажата».** Рестарт происходит мгновенно, и отклик
  ей не делали. Если по плейтесту нажатие окажется «немым», добавляем короткую отдачу.
- **Корона финального экрана в вебе глазами не смотрена.** Оба дефекта веб-сборки от 02.09.2026
  закрыты и проверены в браузере: постобработка исполняется, иконки верны (монета, щебень, доски,
  слитки, свиток). До финального экрана не доигрывалось — корона `degraded`. Случай у неё тот же,
  что у свитка, и замерен отдельно. Разбор — в `docs/archive/changelog.md`.
- **Замок в движении и на телефоне не смотрен.** Снят захватом Game View в Play Mode с двух
  дистанций — на месте, текстура из атласа легла верно, за кромку крышки ничего не свисает.
  Как он читается на дальнем пределе зума и на портрете — `degraded`.
- **`degraded`, ждёт человека:** клик пальцем и мышью после M16, клик по воде и отказ после M17,
  ощущение от глубины и бликов в движении, читаемость лагуны рядом с песком, живая скорость
  падения ресурса, ощущение от улёта за кадр, читаемость на дальнем пределе зума, **читаемость
  HUD на 1080×1920 после M18**, **нажатие кнопок, рестарт обеими кнопками и конфетти в движении
  после M19**, веб-сборка и телефон. Игровое окно редактора на этой машине ландшафтное и
  портрет не переключается из CLI — по такому захвату о раскладке 1080×1920 судить нельзя.
  Веб-сборка не пересобиралась с M15, а M17 трогает `m_RequireDepthTexture` мобильного
  RP-ассета и заводит прозрачный проход, M18 добавляет TMP, SDF-атласы и двухпроходный
  UI-шейдер, M19 — перезагрузку сцены кнопкой; смотреть в вебе надо.

## Что по всем стадиям делает человек

Ручная проверка в Play Mode и на устройстве: касания, щипок, читаемость на 1080×1920, ощущение
от темпа. EditMode-тесты и WebGL-сборку гоняю автоматикой, захват Game View прикладываю, но
скриншот не доказательство интерактивного поведения — такие проверки помечаются `degraded`,
пока человек не посмотрит сам.

---

## Куда что писать

| Что | Куда |
|---|---|
| Ход текущей стадии, чеклист | этот файл |
| Закрытая стадия — итоговая запись | `docs/archive/stages.md` (в конец) |
| Точечная правка вне стадии | `docs/archive/changelog.md` (в начало, с датой) |
| Живая процедура, которую повторяют | `docs/runbooks/` отдельным файлом |
| Изменение правил игры | `docs/MVP_hex_colony.md` — правится вместе с кодом |
| Новый факт о рендере, мешах, гамме, воде, ассетах | `docs/PROJECT_FACTS.md` — в свой раздел |
| Ловушка, на которой встала работа | `AGENTS.md` → «Ловушки», одной строкой со ссылкой на §раздел |

Закрывая стадию: итог переносится в архив, а этот файл **сокращается** до нового состояния.
Он не растёт — если он длиннее двух экранов, значит в нём осело то, чему место в архиве.

## Архив и рунбуки

- `docs/runbooks/webgl-publish.md` — сборка WebGL и публикация в `gh-pages`, целиком.
- `docs/archive/stages.md` — стадии M1–M22: что делалось, чем проверено, что осталось.
- `docs/archive/changelog.md` — датированные правки после MVP (28.08–01.09.2026).
- `docs/archive/stage-journal.md` — журнал стадий M1–M5.
- `docs/archive/START_HERE.md` — инструкция по развёртыванию, устарела, оставлена как след.
