# CLAUDE.md

Правила проекта — в `AGENTS.md` в корне. Прочитай его первым.

## Что читать при старте

Обязательно: `AGENTS.md` (факты проекта и оверрайды).

Остальное — **по необходимости, не заранее**:

| Нужно | Открыть |
|---|---|
| Что делаем сейчас, следующая стадия | `docs/DEVELOPMENT_PLAN.md` (6 КБ) |
| Правила игры, числа, архитектура | `docs/MVP_hex_colony.md` — **по карте документа в шапке**, не целиком |
| Собрать WebGL и опубликовать | `docs/runbooks/webgl-publish.md` |
| Почему что-то сделано именно так | `grep` по `docs/archive/`, см. `docs/archive/README.md` |
| Материал за пределами MVP | `docs/GDD_hex_colony.md` — справка, **не контракт** |

Не загружать `docs/archive/**` в контекст без конкретного вопроса: это 180 КБ истории.

## Быстрая шпаргалка

- Канонический дизайн-контракт: `docs/MVP_hex_colony.md`. Реализуем ровно его, без добавлений.
- Стадия: после MVP, M1–M14 закрыты. Простое рабочее решение важнее расширяемого.
- Не начинать реализацию с уверенностью ниже 90% — сначала прочитать код и документы,
  потом задать вопросы одной пачкой.
- Непрошенная деталь: уверенность 80%+ → предложить и дождаться ответа; ниже → не добавлять.
- Не трогать `.meta`-файлы, `Library/`, `Temp/`, `obj/`, содержимое пакетов.
- Не выдавать скриншот за доказательство поведения. Что не проверено — пометить `degraded`.
